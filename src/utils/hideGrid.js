/**
 * Hides the grid on the specified canvas.
 * @param {HTMLCanvasElement} canvas - The canvas element from which to hide the grid.
 * @returns {void}
 */
export const hideGrid = (canvas) => {
  canvas.remove()
}
